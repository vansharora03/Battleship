A browser game of battleship against the computer. 
Created with HTML, CSS, and JavaScript. 
Tested with Jest.

For a full list of dependencies, consult the package.json file (specifically, under devDependencies).